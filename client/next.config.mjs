/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
      domains: ['www.imghippo.com'], // Add the domain where your images are hosted
    },
  };
  
  export default nextConfig;
  
